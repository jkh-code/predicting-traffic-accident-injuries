CREATE TABLE IF NOT EXISTS crashes_raw (
    crash_record_id VARCHAR,
    crash_date VARCHAR,
    posted_speed_limit VARCHAR,
    traffic_control_device VARCHAR,
    device_condition VARCHAR,
    weather_condition VARCHAR,
    lighting_condition VARCHAR,
    first_crash_type VARCHAR,
    trafficway_type VARCHAR,
    alignment VARCHAR,
    roadway_surface_cond VARCHAR,
    road_defect VARCHAR,
    report_type VARCHAR,
    crash_type VARCHAR,
    damage VARCHAR,
    date_police_notified VARCHAR,
    prim_contributory_cause VARCHAR,
    sec_contributory_cause VARCHAR,
    street_no VARCHAR,
    street_direction VARCHAR,
    street_name VARCHAR,
    beat_of_occurrence VARCHAR,
    num_units VARCHAR,
    most_severe_injury VARCHAR,
    injuries_total VARCHAR,
    injuries_fatal VARCHAR,
    injuries_incapacitating VARCHAR,
    injuries_non_incapacitating VARCHAR,
    injuries_reported_not_evident VARCHAR,
    injuries_no_indication VARCHAR,
    injuries_unknown VARCHAR,
    crash_hour VARCHAR,
    crash_day_of_week VARCHAR,
    crash_month VARCHAR,
    latitude VARCHAR,
    longitude VARCHAR,
    intersection_related_i VARCHAR,
    hit_and_run_i VARCHAR,
    statements_taken_i VARCHAR,
    photos_taken_i VARCHAR,
    crash_date_est_i VARCHAR,
    private_property_i VARCHAR,
    dooring_i VARCHAR,
    work_zone_i VARCHAR,
    work_zone_type VARCHAR,
    workers_present_i VARCHAR,
    rd_no VARCHAR,
    lane_cnt VARCHAR
);

CREATE TABLE IF NOT EXISTS people_raw (
    person_id VARCHAR,
    person_type VARCHAR,
    crash_record_id VARCHAR,
    vehicle_id VARCHAR,
    crash_date VARCHAR,
    city VARCHAR,
    state VARCHAR,
    zipcode VARCHAR,
    sex VARCHAR,
    age VARCHAR,
    drivers_license_state VARCHAR,
    drivers_license_class VARCHAR,
    safety_equipment VARCHAR,
    airbag_deployed VARCHAR,
    ejection VARCHAR,
    injury_classification VARCHAR,
    driver_action VARCHAR,
    driver_vision VARCHAR,
    physical_condition VARCHAR,
    bac_result VARCHAR,
    seat_no VARCHAR,
    hospital VARCHAR,
    ems_agency VARCHAR,
    ems_run_no VARCHAR,
    pedpedal_action VARCHAR,
    pedpedal_visibility VARCHAR,
    pedpedal_location VARCHAR,
    bac_result_value VARCHAR,
    rd_no VARCHAR,
    cell_phone_use VARCHAR
);